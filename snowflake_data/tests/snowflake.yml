version: 2

models:
  - name: staging_orders
    columns:
      - name: order_status
        tests:
          - accepted_values:
              values: ['Completed','Pending', 'Cancelled']
  - name: staging_cutomers
    columns:
      - name: customer_id
        tests:
        - unique